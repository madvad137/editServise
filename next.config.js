/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}
module.exports = {
  reactStrictMode: true,
  images: {
      domains: ["yourDomain.com"],
      formats: ["image/webp"],
  },
};
module.exports = nextConfig
